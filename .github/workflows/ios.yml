name: App de iPhone

# Compila la app, la firma con los certificados reales y publica el .ipa en una
# Release de GitHub. Desde ahí el teléfono se actualiza solo: la app pregunta si
# hay algo más nuevo y, si lo hay, se instala encima sin cable y sin computadora.
#
# Sin los secretos de firma el workflow sigue funcionando: compila y publica un
# .ipa sin firmar, que es lo que hace falta para saber que nada se rompió.

on:
  push:
    branches: [main, 'claude/**']
    paths:
      - 'ios/**'
      - 'sync-core.js'
      - 'business-core.js'
      - 'test/make-conformance-fixtures.js'
      - 'test/make-math-fixtures.js'
      - '.github/workflows/ios.yml'
  pull_request:
    paths: ['ios/**', 'sync-core.js', 'business-core.js', '.github/workflows/ios.yml']
  workflow_dispatch:

concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write        # hace falta para crear la Release con el .ipa

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 45

    env:
      # `secrets` no se puede usar en un `if:` de paso, pero sí para calcular una
      # variable de entorno. Con esto los pasos de firma se saltan solos mientras
      # los certificados no estén cargados.
      HAS_SIGNING: ${{ secrets.IOS_P12_BASE64 != '' && secrets.IOS_MOBILEPROVISION_BASE64 != '' }}
      MARKETING_VERSION: "1.0.0"

    steps:
      - uses: actions/checkout@v4

      - name: Elegir el Xcode más nuevo disponible
        run: |
          echo "Xcodes instalados:"
          ls -1 /Applications | grep -i '^Xcode' || true
          # El más nuevo primero: hace falta el SDK de iOS 26 para Liquid Glass.
          # Si el runner todavía no lo trae, el código compila igual gracias a
          # los #if canImport(FoundationModels) de Views/Theme.swift, sólo que
          # sin los efectos de cristal.
          newest=$(ls -1d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          echo "Usando $newest"
          sudo xcode-select -s "$newest/Contents/Developer"
          xcodebuild -version
          xcodebuild -showsdks | grep -i iphoneos || true

      - name: Comprobar que Swift y la web siguen calculando igual
        run: |
          # Si alguien tocó sync-core.js y no regeneró los casos, esto lo caza
          # antes de que el teléfono y la laptop empiecen a discrepar.
          node test/make-conformance-fixtures.js
          node test/make-math-fixtures.js
          if ! git diff --quiet -- ios/OlivoLioraCore/Tests/OlivoLioraCoreTests/; then
            echo "::error::Los casos de conformidad están desactualizados."
            echo "Corre 'node test/make-conformance-fixtures.js' y 'node test/make-math-fixtures.js' y súbelos."
            git diff -- ios/OlivoLioraCore/Tests/OlivoLioraCoreTests/
            exit 1
          fi

      - name: Pruebas del núcleo (modelos, cuentas y sincronización)
        working-directory: ios/OlivoLioraCore
        run: swift test

      - name: Instalar XcodeGen
        run: brew install xcodegen

      - name: Generar el proyecto de Xcode
        working-directory: ios
        run: xcodegen generate

      # ----------------------------------------------------------------------
      # Sin certificados: se compila igual, sólo que el .ipa no se puede instalar.
      # ----------------------------------------------------------------------

      - name: Compilar sin firmar
        if: env.HAS_SIGNING != 'true'
        working-directory: ios
        run: |
          set -o pipefail
          xcodebuild \
            -project OlivoLiora.xcodeproj \
            -scheme OlivoLiora \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            MARKETING_VERSION=$MARKETING_VERSION \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            build | xcpretty || \
          xcodebuild \
            -project OlivoLiora.xcodeproj \
            -scheme OlivoLiora \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            MARKETING_VERSION=$MARKETING_VERSION \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            build

      - name: Empaquetar el .ipa sin firmar
        if: env.HAS_SIGNING != 'true'
        working-directory: ios
        run: |
          APP="build/Build/Products/Release-iphoneos/OlivoLiora.app"
          test -d "$APP" || { echo "::error::No se generó $APP"; ls -R build/Build/Products || true; exit 1; }
          # Un .ipa es sólo un zip con la app dentro de una carpeta Payload.
          rm -rf Payload && mkdir Payload
          cp -R "$APP" Payload/
          zip -qry OlivoLiora.ipa Payload
          rm -rf Payload
          echo "Tamaño: $(du -h OlivoLiora.ipa | cut -f1)"

      # ----------------------------------------------------------------------
      # Con certificados: firma de verdad y Release instalable.
      # ----------------------------------------------------------------------

      - name: Firmar para cada iPhone
        if: env.HAS_SIGNING == 'true'
        env:
          P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
          PROFILE_BASE64: ${{ secrets.IOS_MOBILEPROVISION_BASE64 }}
          DEVICE_NAME: ${{ secrets.IOS_DEVICE_NAME }}
          P12_BASE64_2: ${{ secrets.IOS_P12_BASE64_2 }}
          P12_PASSWORD_2: ${{ secrets.IOS_P12_PASSWORD_2 }}
          PROFILE_BASE64_2: ${{ secrets.IOS_MOBILEPROVISION_BASE64_2 }}
          DEVICE_NAME_2: ${{ secrets.IOS_DEVICE_NAME_2 }}
        run: |
          set -euo pipefail

          # El llavero vive sólo lo que dura este trabajo y se borra al final.
          # La contraseña es de usar y tirar: no protege nada más allá de estos
          # pocos minutos dentro de una máquina que se destruye después.
          KEYCHAIN="$RUNNER_TEMP/olivo-signing.keychain-db"
          KEYCHAIN_PASSWORD=$(uuidgen)
          echo "KEYCHAIN=$KEYCHAIN" >> "$GITHUB_ENV"
          echo "::add-mask::$KEYCHAIN_PASSWORD"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security list-keychains -d user -s "$KEYCHAIN" $(security list-keychains -d user | tr -d '"')

          # Los servicios de firma tipo KravaSign entregan el .p12 ya listo, sin
          # contraseña, así que el secreto de la contraseña puede no existir. Se
          # prueba lo que haya y, si no abre, la contraseña vacía: dos intentos
          # sobre su propio archivo, para no obligar a nadie a averiguar antes
          # cuál de los dos casos es el suyo.
          importar_p12 () {
            local archivo="$1" clave="$2" etiqueta="$3"
            local mete=(security import "$archivo" -k "$KEYCHAIN" -f pkcs12
                        -T /usr/bin/codesign -T /usr/bin/security)
            if "${mete[@]}" -P "$clave"; then
              return 0
            elif [ -n "$clave" ] && "${mete[@]}" -P ""; then
              echo "::warning::El .p12 de $etiqueta no llevaba contraseña; ese secreto sobra."
              return 0
            fi
            echo "::error::No se pudo abrir el .p12 de $etiqueta."
            echo "Si KravaSign te lo dio sin contraseña, borra el secreto de la contraseña."
            echo "Y comprueba que pegaste el base64 entero, sin recortar."
            return 1
          }

          # Cada certificado de KravaSign vale para UN iPhone: el perfil lleva
          # dentro la lista de dispositivos que acepta, y en cualquier otro la
          # instalación falla. Por eso se firma una vez por teléfono.
          VARIANTES=()
          preparar () {
            local n="$1" p12="$2" clave="$3" perfil="$4" nombre="$5"
            [ -n "$p12" ] && [ -n "$perfil" ] || return 0
            echo "$p12"    | base64 --decode > "$RUNNER_TEMP/cert-$n.p12"
            echo "$perfil" | base64 --decode > "$RUNNER_TEMP/perfil-$n.mobileprovision"
            importar_p12 "$RUNNER_TEMP/cert-$n.p12" "$clave" "${nombre:-teléfono $n}"
            rm -f "$RUNNER_TEMP/cert-$n.p12"
            VARIANTES+=("$n|$nombre")
          }

          preparar 1 "$P12_BASE64" "${P12_PASSWORD:-}" "$PROFILE_BASE64" "${DEVICE_NAME:-}"
          preparar 2 "${P12_BASE64_2:-}" "${P12_PASSWORD_2:-}" "${PROFILE_BASE64_2:-}" "${DEVICE_NAME_2:-}"

          # Sin esto macOS pregunta por la contraseña del llavero a mitad de la
          # firma, y en un runner sin pantalla eso es un cuelgue de 6 horas.
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN" >/dev/null

          echo "Identidades disponibles:"
          security find-identity -v -p codesigning "$KEYCHAIN" || true

          rm -f "$RUNNER_TEMP/variantes.jsonl"
          touch "$RUNNER_TEMP/variantes.jsonl"
          for v in "${VARIANTES[@]}"; do
            n="${v%%|*}"; nombre="${v#*|}"
            # Con dos certificados hay dos identidades en el llavero, y cada
            # .ipa tiene que firmarse con la que va con su perfil. Se busca la
            # que el propio perfil lleva dentro, no la primera que aparezca.
            propia=$(python3 ios/identity-for-profile.py \
                       "$RUNNER_TEMP/perfil-$n.mobileprovision" "$KEYCHAIN")
            echo "Perfil $n → identidad: $propia"

            linea=$(ios/sign.sh "$RUNNER_TEMP/perfil-$n.mobileprovision" "$propia" \
                      "$KEYCHAIN" "$GITHUB_WORKSPACE/ios/salida" \
                      "${{ github.run_number }}" "$MARKETING_VERSION")
            python3 -c 'import json,sys; d=json.loads(sys.argv[1]); d["nombre"]=sys.argv[2] or "este iPhone"; print(json.dumps(d, ensure_ascii=False))' \
              "$linea" "$nombre" >> "$RUNNER_TEMP/variantes.jsonl"
          done

          echo "Firmado:"
          cat "$RUNNER_TEMP/variantes.jsonl"
          python3 ios/check-variants.py "$RUNNER_TEMP/variantes.jsonl"

      - name: Guardar los .ipa como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: OlivoLiora-ipa
          path: |
            ios/OlivoLiora.ipa
            ios/salida/*.ipa
          if-no-files-found: error
          retention-days: 90

      # ----------------------------------------------------------------------
      # Publicar: de aquí es de donde el teléfono se baja la actualización.
      # ----------------------------------------------------------------------

      - name: Publicar la versión
        if: env.HAS_SIGNING == 'true' && github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        working-directory: ios
        run: |
          set -euo pipefail
          BUILD=${{ github.run_number }}
          TAG="ios-$MARKETING_VERSION-$BUILD"
          REPO="${{ github.repository }}"

          # `releases/latest/download/<archivo>` es una dirección fija que
          # siempre apunta a lo último publicado, así que ni el teléfono ni el
          # servidor tienen que consultar la API de GitHub ni guardar llaves.
          python3 "$GITHUB_WORKSPACE/ios/make-version.py" \
            "$RUNNER_TEMP/variantes.jsonl" \
            "https://github.com/$REPO/releases/latest/download" \
            "$MARKETING_VERSION" "$BUILD" "${{ github.sha }}" > salida/version.json
          cat salida/version.json

          gh release create "$TAG" \
            --repo "$REPO" \
            --title "Olivo & Liora $MARKETING_VERSION ($BUILD)" \
            --notes "$(git log -1 --pretty=%s)" \
            --latest \
            salida/*.ipa salida/version.json

          {
            echo "### La app quedó publicada"
            echo ""
            echo "Para instalarla en un iPhone, ábrelo **en ese iPhone**:"
            echo ""
            echo "<https://olivo-liora.vercel.app/instalar.html>"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Cerrar el llavero
        if: always() && env.HAS_SIGNING == 'true'
        run: |
          security delete-keychain "$KEYCHAIN" 2>/dev/null || true
          rm -f "$HOME/Library/MobileDevice/Provisioning Profiles/$UUID.mobileprovision" 2>/dev/null || true
          rm -f "$HOME/Library/Developer/Xcode/UserData/Provisioning Profiles/$UUID.mobileprovision" 2>/dev/null || true
